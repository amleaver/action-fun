name: Deploy

on:
  workflow_dispatch:
#    branches:
#      - WEBAPI-1554_Resurrect-HTML2PDF-lambda-deployments-via-GitHub-action_Alex-Leaver
#      - main
    inputs:
      environment:
        description: Environment
        required: true
        type: environment

jobs:
  deploy:
    runs-on: ubuntu-latest
#    environment: ${{ github.event.inputs.environment }}

    steps:
      - run: |
          echo ${{secrets.TEST }} | sed 's/./& /g'